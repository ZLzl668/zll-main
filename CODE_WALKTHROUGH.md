# Kender 代码讲解稿（面试专用）

> 用法：这份文档不是给你背的"答案"，是帮你**把代写代码读透**的地图。
> 每个模块讲清"它干什么、关键在哪、为什么这么写、会被怎么追问"。
> 面试前过两遍，确保你能脱稿讲出 `agent.py` 和 `tools.py`。

---

## 一、一句话项目定位（开场先说这个）

> "Kender 是一个基于 AgentScope 框架、用通义千问驱动的**生活助手 Agent**。
> 它的核心不是简单的聊天，而是具备**工具调用（ReAct）能力**——模型能自主决定要不要联网搜最新信息；
> 同时有**跨会话长期记忆**，能记住用户的名字和偏好；并且用 Gradio 做了 Web 界面、用 Docker 可以一键部署。"

这句话覆盖：框架、模型、Agent 本质、记忆、工程化。开场就把格局立住。

---

## 二、整体数据流（要求能白板画出来）

```text
用户消息
   ↓
main.py 入口（--web 走 Gradio / 否则走 CLI）
   ↓
ui.py：组装消息（拼接联网偏好提示 + 注入文档内容）→ 调 agent.get_reply
   ↓
agent.py：
   1. 拼 system prompt（记忆回灌 + 真实日期 + 工具说明）
   2. ReActAgent 推理 → 自主决定是否调用 search_web
   3. 提取回复文本
   4. 顺手抽取：用户名字 + key_facts（用户事实）
   ↓
memory.py：把 user_name / key_facts / chat_history 存成 JSON
   ↓
回复返回界面
```

**可复述版口播**："用户说话后，界面层先做一些预处理——比如把文档内容拼进消息、把联网偏好作为提示。然后交给 agent 层：先用 system prompt 把历史记忆和今天的真实日期告诉模型，模型用 ReAct 方式推理，需要最新信息时就自己调用 search_web 工具。拿到回复后，我再额外用一次轻量 LLM 从对话里抽取用户的事实写进记忆，这样下次对话就更懂用户了。"

---

## 三、逐模块讲解

### 1. `main.py` —— 入口与双界面

**职责**：根据命令行参数决定启动 Web 还是命令行。

**关键代码点**：
- `sys.argv[1] == "--web"` → 走 Gradio；否则走 CLI 循环。
- Web 启动时读 `GRADIO_SERVER_NAME` 环境变量决定监听地址（默认 `127.0.0.1`），Docker 里我们设成了 `0.0.0.0` 才能外部访问。

**为什么这么写**：一套代码两种用法，本地调试用 CLI 最快，演示用 Web 最直观。

**高频追问**：
- Q：Web 和 CLI 有什么区别？
  A：只是交互入口不同，核心逻辑都走 `agent.get_reply`，UI 只是把消息传进去、把回复显出来。
- Q：为什么 Docker 里要监听 0.0.0.0？
  A：容器里 127.0.0.1 只接受容器内访问，外部（宿主机/浏览器）连不上，必须监听 0.0.0.0 才能被映射端口访问。

---

### 2. `src/ui.py` —— Gradio 界面层

**职责**：搭建聊天界面，做**消息预处理**，调用 agent。

**关键代码点**：
- `create_ui()` 用 `gr.Blocks` 搭界面，有对话窗、联网勾选框、文件上传、输入框。
- `chat_with_kender` 里两处预处理：
  1. 勾选联网时，在消息后追加一句"用户希望优先使用联网搜索"的**偏好提示**（注意：只是提示，最终是否联网还是模型决定）。
  2. 上传文件时，用 `read_document` 读出前 3000 字，作为 `【文档内容】` 拼进消息。
- 内部用 `asyncio.run(get_reply(...))` 调用异步的 agent。

**为什么这么写（两个重要设计决策）**：
- **文档不注册成工具**：模型感知不到本地文件路径，所以文件读取放在应用层（UI 上传时就读好），再把内容塞进消息。这是"应用层预处理"而非"Agent 工具"的典型取舍。
- **联网不是关键词硬触发**：旧版是界面按关键词手动拼搜索结果，新版改为把决策权交给模型——是否调用 `search_web` 由 ReAct 推理决定，界面只给偏好提示。

**高频追问**：
- Q：文件内容为什么只取前 3000 字？
  A：控制送入模型的上下文长度，避免长文档撑爆 token、拖慢响应；真实场景可按需分页或摘要。
- Q：为什么用 `asyncio.run` 而不是直接 await？
  A：Gradio 的事件回调是同步函数，而 agent 的 `reply` 是 async 的，所以在同步回调里用 `asyncio.run` 包一层执行。

---

### 3. `src/agent.py` —— Agent 核心（最重要，重点背）

**职责**：构建 Agent、注册工具、处理一轮对话、抽取记忆。

#### 3.1 `create_agent(memory)` —— 组装 Agent
- 用 `build_memory_prompt(memory)` 把记忆拼进系统提示。
- `Toolkit().register_tool_function(search_web)`：**真正把工具注册进 Agent**。AgentScope 会读函数的**类型注解 + docstring** 自动生成工具 schema，模型推理时自主决定是否调用。
- 模型用 `DashScopeChatModel`（通义千问 `qwen-plus`）。
- `ReActAgent`：reason + act 循环——先思考要用什么工具，调了再看结果，再决定下一步或作答。
- system prompt 里**硬写了真实日期**，并明确告诉模型"不要质疑这个日期"——这是修过的一个 bug（模型曾误以为 2026 年是未来而拒绝回答）。

#### 3.2 `_extract_text(reply)` —— 统一回复提取
- 不同版本 / 是否触发工具，AgentScope 的回复可能是 str、dict、或 content blocks 列表。
- 这里分三步走：先试 `.text` 属性，再试 `get_content_blocks`，最后回退到 `.content`，统一转字符串。
- **为什么**：旧代码直接当字符串存，遇到 dict/list 就把对象结构塞进了记忆文件，是个 bug。这版做了兼容。

#### 3.3 `_extract_key_facts(...)` —— 持续认知（讲清楚边界！）
- 每轮对话后，额外用一次 LLM，从"用户说了什么 + 助手回了什么"里，**抽取关于用户的事实/偏好**，要求返回 JSON 数组。
- 做去重、限制最多保留 15 条，写进 `memory["key_facts"]`。
- **⚠️ 边界话术（必背）**："这不是模型真正的学习，是**显式记忆机制**——我用一次额外 LLM 调用把用户事实结构化存起来，下次作为系统提示回灌。模型权重没变，是应用层在模拟'越聊越懂你'。"

#### 3.4 `get_reply(...)` —— 一轮对话主流程
- 把"真实日期 + 用户问题"封装成 `Msg` 发给 agent。
- 提取回复文本。
- 用正则从用户输入里抽名字（如"我叫杨恺"）→ 存 `user_name`（简单够用，后续可换 LLM 结构化抽取）。
- 调 `_extract_key_facts` 抽事实 → 存记忆。
- 把本轮对话追加进 `chat_history`（截断到最近 100 条）。
- `save_memory` 落盘。

**高频追问（这块最爱被深挖）**：
- Q：ReAct 具体怎么工作的？
  A：模型先输出"思考"和"要调用的工具+参数"，框架执行工具拿到观测结果，再把结果喂回模型，模型继续思考或给出最终答案，循环直到结束。
- Q：工具是怎么被模型知道的？
  A：注册时框架读函数的签名和 docstring 生成 JSON schema，作为工具的"说明书"随系统提示一起给模型，模型据此决定调不调、传什么参。
- Q：key_facts 算不算模型有记忆/会学习？
  A：（用上面的边界话术）不算真正的权重级学习，是应用层的显式记忆回灌，实事求是讲。
- Q：为什么每轮都要额外调一次 LLM 抽事实？成本高吗？
  A：会多一次调用，但事实抽取用很短的 prompt、且只在有用户新输入时做；对演示和真实辅助场景成本可接受，也可改为异步/批量。

---

### 4. `src/tools.py` —— 工具函数

三个工具，注意**注册与否**的区别：

- `search_web(query, max_results=3)`：**注册为 Agent 工具**。用 `ddgs`（DuckDuckGo）联网搜，返回标题/摘要/链接文本。docstring 写得很详细，因为框架靠它生成工具说明。
- `read_document(file_path)`：**不注册为工具**，由 UI 层调用，支持 txt/docx/pdf，取前 5000 字。理由同上——模型感知不到本地路径。
- `get_current_date()`：返回"今天是 X 年 X 月 X 日，星期 X"，帮模型感知时间，避免幻觉日期。

**高频追问**：
- Q：为什么 search_web 注册成工具，read_document 不注册？
  A：search_web 的输入是用户问题关键词，模型能生成；read_document 需要本地真实文件路径，模型生成不了也不该知道，所以放在应用层处理。
- Q：联网搜索用的什么？为什么不接搜索引擎 API？
  A：用 DuckDuckGo 的 `ddgs` 库，免 API Key、免注册，适合 demo；生产可换 SerpAPI / Bing 等更稳定的服务。

---

### 5. `src/memory.py` —— 记忆持久化

**职责**：把记忆读写成 JSON 文件 `data/kender_memory.json`。

- `load_memory()`：文件不存在/损坏时返回默认结构 `{"user_name": None, "key_facts": [], "chat_history": []}`。
- `save_memory()`：`os.makedirs("data")` 保证目录存在，JSON 落盘（中文不转义）。
- `build_memory_prompt(memory)`：把 user_name 和最近 5 条 key_facts 拼成提示文本，注入 system prompt。

**高频追问**：
- Q：记忆存在哪？进程重启还在吗？
  A：本地 JSON 文件，重启仍在；但部署到无状态容器（如 HF Spaces）会随容器重置，属预期。
- Q：chat_history 现在真正用上了吗？
  A：目前它落盘了但**没有完全回灌成短期记忆**——这是已知的待改进项。我在 README 的"后续计划"里也写了。诚实讲这一点，比硬吹强。

---

## 四、硬核追问清单（建议对着练 3 遍）

1. **ReAct 和普通的"调用一次工具"有什么区别？**
   → ReAct 是"思考-行动-观察"循环，可能多次调工具、根据结果调整；普通 Chain 是线性管道。
2. **如果让用户问一个需要实时数据的问题，你的 Agent 一定会联网吗？**
   → 不一定"一定"，是由模型根据工具说明自主决定；我在 UI 加了偏好提示提高概率，但决策权在模型。
3. **RAG 和你的"文档上传"有什么不同？**
   → 我现在的文档是整段塞进 prompt（简单注入）；RAG 是先切片、向量化建库、检索 Top-K 再注入，更适合大文档和知识库。我另有两个 demo 专门做 RAG（可展开讲）。
4. **你的项目最大短板是什么？**
   → 诚实说两点：chat_history 还没真正回灌成短期记忆；key_facts 是显式记忆不是真学习。已经在改进计划里。
5. **为什么选 AgentScope 而不是 LangChain？**
   → AgentScope 对 ReAct Agent 和 Toolkit 的封装更轻、上手快；LangChain 生态大但版本碎片化严重（我另一个 demo 踩过 1.x 的坑）。按场景选。

---

## 五、红线提醒（绝对别说）

- ❌ 别说"模型会自主学习 / 有长期记忆能力"——key_facts 是显式回灌，说成真学习会被追问死。
- ❌ 别把"文档上传"说成"RAG"——两者原理不同，面试官一眼能看穿。
- ❌ 别假装代码全是你从零手写——被问到具体某行为什么这么写答不上来更尴尬。**正确姿态**：承认是迭代打磨出来的，但每个模块你都读得懂、讲得清（这正是这份稿子的目的）。
- ✅ 被问到不会的：**"这块我目前是这么理解的……实际生产里一般会……"**，把话题引到你熟悉的工程权衡上。

---

## 六、给你的私房建议

你最大的风险不是项目本身，是**代码不是你独立写的，怕被问穿**。
破解方法只有一条：**把这份稿子对应到真实代码逐行看一遍**。看不懂的地方（比如 ReActAgent 内部、AgentScope 的 Msg 结构）现在就搜一下或问我，别等到面试前夜才慌。

能讲清楚"为什么这么写"，比"写了什么"重要十倍。
